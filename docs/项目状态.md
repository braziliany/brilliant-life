# 项目状态

更新日期：2026-09-13

璀璨人生是部署在 Cloudflare Workers、由 Access 保护的单人数字生活档案。当前生产版本以 `main` 为准，Health、Calendar、Career、Salary、Life Finance、Annual Archive 与 Life Insights Phase 1 均已上线。

## 当前阶段

- v1.4 已完成领域化、D1 可靠性和统一验证基线。
- v1.5 已完成年度档案、实际财务流水接入、健康同步可信语义及 Life Insights Phase 1。
- Life Timeline Phase 1 已通过 Desktop / iPhone 验收并上线；它只组织 Career、已保存 Salary 与 Annual Archive，不持久化、不提供编辑，也不接入 Finance、Health、Calendar 日级数据或 Life Insights。
- Timeline Phase 2 已暂缓：Timeline 保持跨年份、低频、时间定位的人生事实索引，Annual Archive 继续承担单一年份的完整事实回顾。只有真实出现“重要人生节点无处记录”的需求时，才重新评估 Life Event。
- Calendar Widget Phase 1 已完成正式部署与生产安全验收：只读机器接口复用 Calendar domain，独立 Service Token 仅能访问精确 Widget path，父级 Pulse 路由继续要求 Owner 登录；canonical workers.dev 与 Version/Preview URL 均已关闭并通过外部黑盒验证。
- Health Ingest 已完成 machine-entry 安全回归修复与 iPhone 真机验收：正式入口为 `POST /api/health/ingest`，独立 Health Service Token 与 Worker `X-API-Key` 分层鉴权；Calendar / Health token 双向隔离通过，Phase 1 Production Acceptance = PASS。
- 2026 钱迹 canonical baseline rebuild 的一次性 executor 与事务测试已经就绪；2026-09-13 Production D1 最小只读复测已恢复，平台阻塞解除。baseline rebuild 仍未执行，必须等待新的人工批准，生产写入为 0。

## 生产基线

- 线上入口：`https://pulse.sophier.org`
- Worker：`pulse-health-dashboard`
- 数据库：Cloudflare D1，binding 为 `DB`
- 登录：Cloudflare Access One-time PIN，仅授权邮箱，应用会话 14 天
- Calendar Widget 机器入口：`GET /api/v1/calendar/widget` + 独立 Access Service Token，仅限该 path
- Health Ingest 机器入口：`POST /api/health/ingest` + 独立 Health Service Token + `X-API-Key`，仅限该 path
- Worker 公共入口：`workers_dev = false`、`preview_urls = false`
- Dashboard/private read：`pulse.sophier.org/*` + Owner Access；`/api/health` 不作为机器写入入口
- 本地环境：Node.js 22.x
- 发布门禁：`npm run verify` 与 `git diff --check`
- 页面入口：全站使用唯一正式顶部导航；移动端使用同一组入口的原生展开菜单。

精确版本号、数据量和金额属于发布时点信息，保存在[里程碑](./05-里程碑/)而非本页。

## 不可破坏的语义

- Health 缺失不等于零；显式零值才是零。
- 已保存 Salary 快照不等于按当前日历计算的预计工资。
- Salary 收入事实不等于 Life Finance 中的实际收入流水，两者不自动对账。
- Life Finance 截止日期不等于 Annual `asOf`，两者必须分别表达。
- 当前年度 Annual 是截至 `asOf` 的 YTD 视图，未来日历配置须明确标记。
- Life Insights 是由已存在事实临时计算的派生结果，不持久化、不反写源数据。
- Life Timeline 同样只组织现有稳定事实，不创建新的事件事实层或时间线快照。
- Timeline 不因 Calendar Notes 或 Finance 候选记录扩展成另一份 Annual Archive，也不提供 CRUD、AI summary 或 importance score。
