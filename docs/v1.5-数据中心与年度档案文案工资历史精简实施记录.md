# v1.5 数据中心与年度档案文案 / 工资历史精简实施记录

## 日期

2026-08-11

## 目标

- 工资历史默认只展示月份、工作日、额外收入和实发金额。
- 完整工资组成保留在每月可展开详情中。
- 年度页面顶部保留年份、进行状态和动态 `asOf` 日期，移除开发者式文案。

## 修改范围

- `app/features/salary/SalaryDashboard.tsx`
- `app/features/annual/AnnualReportPage.tsx`
- `app/globals.css`
- `tests/data-center-ia.test.mjs`
- `tests/annual-page-contract.test.mjs`

未修改 Salary / Annual domain、API、D1、migration、工资规则、Snapshot 或 LifeEvent。

## 工资历史最终结构

默认列表：

```text
2026 年 08 月
21 个工作日 · 额外收入 ¥0.00

实发  ¥5,625.65
```

展开详情：应发、固定扣除、请假扣款、个税、额外收入、奖金。所有金额直接读取已保存的 `SalaryRecord`，没有重新计算历史工资。

## 年度顶部最终结构

```text
我的 2026
2026 年度档案 · 进行中
截至 2026-08-11
```

年份、状态与日期均来自当前 `AnnualSummaryDraft` 数据；生产界面不显示“个人数字生命档案”“实时草稿”或计算版本号。

## 其他文案清理

- “仅工资快照”改为“来自已保存记录”。
- “实时计算”改为“计算”。
- 年度记录说明不再显示内部计算版本，改为动态截至日期。

## 验证

- `npm run build`：通过。
- `npm run verify`：通过。
- 领域与契约测试：79/79。
- migration：2/2。
- Worker 渲染：1/1。
- `git diff --check`：通过。
- 响应式契约覆盖 Desktop、iPhone / PWA 的工资列表和年度页面布局。

## 发布状态

本轮按任务要求仅保留本地修改，未提交、未推送、未部署。
