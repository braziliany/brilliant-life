# 2026-09 开发记录

- 实现独立的钱迹历史 reconciliation engine：复用现有 JSON/XLSX adapter 与 normalizer，普通 import 继续保持 upsert、no-delete、保留人工字段的原语义。
- Planner 以明确的 `source=qianji` 与日期范围生成 dry-run buckets；canonical identity 重复、歧义、缺失、rekey 与疑似重复不会自动清理。
- 私有 manifest 记录 canonical SHA-256、适配器 commit、bucket counts、scoped precondition hash 与逐项 resolution；apply 只接受完整 reviewed manifest，production apply 在本阶段硬禁用。
- 合成 D1 测试覆盖 update / insert / approved rekey / approved delete、人工语义资产保留、账务快照、时区候选识别与 checkpoint 恢复演练。
- No migration；未执行生产 reconciliation 或任何生产 Finance 写入。
- 建立 QianJi Trusted Import Contract：现有 JSON/XLSX adapter 增加同源 inspection，网页在 POST 前自动校验，另提供 finance:validate 轻量 CLI；通过后直接执行既有 Normal Import，不要求 reconciliation dry-run。
- Normal Import 永久保持同 ID 更新、新 ID 新增、文件缺失记录忽略、人工 override / semantic note / relation IDs 保留；不启用 missing 分析、fuzzy rekey、manifest 或自动删除。Historical Reconciliation 仍只用于用户显式声明的 canonical 历史维护。
- 合成回归覆盖真实 OOXML shared-string 空值结构（索引 23 指向空字符串）、+08:00 时间、五种交易类型、账户/分类/备注/标签、重复或空 ID、坏日期/金额/类型、必要表头与文件结构阻塞，以及真实 D1 normal-import 幂等与人工字段保留。
- 本轮 No migration，Production writes = 0；真实 canonical、production export 与 reconciliation manifest 均未加入 Git。
