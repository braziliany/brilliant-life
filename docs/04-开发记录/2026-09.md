# 2026-09 开发记录

- 实现独立的钱迹历史 reconciliation engine：复用现有 JSON/XLSX adapter 与 normalizer，普通 import 继续保持 upsert、no-delete、保留人工字段的原语义。
- Planner 以明确的 `source=qianji` 与日期范围生成 dry-run buckets；canonical identity 重复、歧义、缺失、rekey 与疑似重复不会自动清理。
- 私有 manifest 记录 canonical SHA-256、适配器 commit、bucket counts、scoped precondition hash 与逐项 resolution；apply 只接受完整 reviewed manifest，production apply 在本阶段硬禁用。
- 合成 D1 测试覆盖 update / insert / approved rekey / approved delete、人工语义资产保留、账务快照、时区候选识别与 checkpoint 恢复演练。
- No migration；未执行生产 reconciliation 或任何生产 Finance 写入。
- 建立 QianJi Trusted Import Contract：现有 JSON/XLSX adapter 增加同源 inspection，网页在 POST 前自动校验，另提供 finance:validate 轻量 CLI；通过后直接执行既有 Normal Import，不要求 reconciliation dry-run。
- Normal Import 永久保持同 ID 更新、新 ID 新增、文件缺失记录忽略、人工 override / semantic note / relation IDs 保留；不启用 missing 分析、fuzzy rekey、manifest 或自动删除。Historical Reconciliation 仍只用于用户显式声明的 canonical 历史维护。
- 合成回归覆盖真实 OOXML shared-string 空值结构（索引 23 指向空字符串）、+08:00 时间、五种交易类型、账户/分类/备注/标签、重复或空 ID、坏日期/金额/类型、必要表头与文件结构阻塞，以及真实 D1 normal-import 幂等与人工字段保留。
- 本轮 No migration，Production writes = 0；真实 canonical、production export 与 reconciliation manifest 均未加入 Git。
- 完成 Life Timeline Phase 1：新增 `/timeline`，以年份分组组织 Career 工作经历、已保存 Salary 快照与 Annual Archive 年度节点；使用独立 derived/read model，保持月/年精度、确定性倒序及来源链接。
- 时间线只读且不持久化，不新增表、migration 或 mutation；Salary 直接读取历史快照，Annual 复用 `annual-summary-v3` 的 YTD/完整年度语义，未接入 Finance transaction、Health/Calendar 日数据或 Life Insights。
- Salary UI 统一采用“工资”作为最终金额的用户文案，同时保留应发工资、全部扣除、计税收入与个人所得税的独立语义；工资历史改为按时间展示全部已保存 `netSalary` 快照的单线趋势，不补齐缺失月份、不重算历史数据。
- 正式顶部导航统一收纳首页、健康、时间、职业、财务记录、工资、年度档案与时间线；首页旧领域快捷导航及其专属间距/样式已移除，移动端复用原生 disclosure 展开全部入口。
- 首页《原神》名句改为显式版本时间窗驱动：按 Asia/Shanghai 自然日，从当前主版本的上一主版本精选池稳定选取；未知未来日期回退至最新已确认的上一主版本池，不进行网络查询或周期推算。

## 2026-09-10 Life Timeline Phase 1

- `949e0fad261a6a9290d09668c53d62a72011ae8c` 新增 `/timeline` 派生只读视图，`10737e0f07b9aec2acbaaf610e7b2623f63a4466` 统一工资节点文案；来源严格限定为 Career、已保存 Salary 与 Annual Archive。
- 时间线保持 month/year 真实精度、确定性倒序与领域回链，不新增 Timeline CRUD、Life Event 表、持久化快照、AI summary 或评分。Desktop / iPhone Device Acceptance = PASS。

## 2026-09-11 Salary UI / Trend Polish

- `7952ef6b8a85700428d52feb46305eb8b8e8e7d0` 将最终工资的主要用户文案统一为自然“工资”语义，并把工资历史改为只读取已保存 `netSalary` 快照的单序列折线，不补缺失月份、不重算历史。
- `c7b9fbd1019e4d8c737ed0290e87e6fe6ae3a6ca` 让单点状态在 96px plot area 内水平、垂直居中；`358a44d96dd26408bb57d4490013d2525bf617e9` 为多点趋势使用带最小跨度与上下 padding 的动态 Y domain，避免 `0 → maxSalary` 把折线挤在顶部，也避免微小差异被夸张。最终 iPhone 验收 PASS。

## 2026-09-11 Timeline + Salary Production Deploy

- 部署 commit `358a44d96dd26408bb57d4490013d2525bf617e9`，Worker Version `5f16deb7-7742-4228-941b-bd7ef6332cb0`；Deployment = PASS，No migration，Production DB writes = 0。
- 当次已认证自动 smoke 被本地 browser-control 故障阻塞，不是生产应用 regression；Timeline 与 Salary 的 Desktop / iPhone 人工验收均已通过。

## 2026-09-11 Formal Navigation + Genshin Quote

- `bcedce7a4900688adf8be0050c266c4d8b80e33c` 将双层导航收敛为唯一正式顶部导航：`首页 / 健康 / 时间 / 职业 / 财务记录 / 工资 / 年度档案 / 时间线`。旧 `DataQuickNav` 组件、DOM 与专属样式完整删除；桌面保留绿色短下划线，移动端使用单一原生 disclosure 和至少 44px 触控区，并修复移除 hash 后仍停留旧领域状态的问题。
- 首页《原神》名句不再使用固定“每日一言”：显式本地版本窗维护 `6.x → 5.x`、`7.x → 6.x`，按 Asia/Shanghai 自然日确定性选择上一主版本精选池；不发起运行时外部请求，未知未来日期安全回退。来源格式为 `— 角色名 ·《原神》N.x`。Desktop / iPhone Device Acceptance = PASS。

## 2026-09-11 Formal Navigation + Genshin Production Deploy

- 部署 commit `bcedce7a4900688adf8be0050c266c4d8b80e33c`，Worker Version `d42a5a2d-0b71-49f5-84d5-4500e854b0cf`，时间 `2026-09-11 19:23:10 +08:00`。Combined targeted 29/29、full verify 197/197、migration 2/2、Rendered Worker 1/1、build 与 `git diff --check` 全部通过。
- Deployment = PASS，Cloudflare Access protected-resource challenge / login redirect 符合预期；部署产物确认新主导航与版本化名句存在，`DataQuickNav` 和旧“每日一言”不存在。已认证 browser automation 被 Windows browser-control sandbox 故障阻塞，不是已观察到的生产应用失败。No migration，Production DB writes = 0。

## 2026-09-11 QianJi D1 Retry

- 已验证 canonical 为 1,132 条，SHA-256 `5d724afeadcb7b21e23529713f4921c80f6dd03694161eca1571da51cf130826`，duplicate / invalid / generated IDs 均为 0；guarded baseline rebuild executor 与 transactional rollback tests 均为 PASS。
- 生产 scoped preflight 的只读访问连续被 Cloudflare remote D1 internal error 阻塞，最近 error IDs 为 `uglao8tmgvi3h5d39bragok4`、`escqbnqelof0c6flvorpsma7`、`piu87ditcqdbbe35d92oekep`。状态是 READY / PLATFORM BLOCKED，不是 rebuild 失败；Production DB writes = 0，Baseline rebuild = NOT EXECUTED。

## 2026-09-11 Timeline Phase 2 Product Decision

- Timeline Phase 2 暂缓。Timeline 继续作为跨年份、低频、时间定位的人生事实索引；Annual Archive 继续负责单一年份的完整事实回顾，避免 Calendar Notes / Finance candidates 与 Annual 重叠并违背 Less is More。
- 当前不扩 Timeline 数据源，不新增 `life_events`、Timeline CRUD、AI summary 或 importance score。只有真实出现“重要人生节点无处记录”的需求时，再重新评估 Life Event。

## 2026-09-12 Salary History Maintenance Production Deploy

- 部署 commit `4d27596053f052879a481132f89700505ceb36f4`，Worker Version `a9f9fd51-4d98-4d3d-93e9-f1b12796b22a`，时间 `2026-09-12 23:24:40 +08:00`。Salary targeted 41/41、full verify 209/209、migration 2/2、Rendered Worker 1/1、build 与 `git diff --check` 全部通过。
- 历史工资成为可由用户主动纠正或补录的已保存事实；重复月份受保护，月份本身不可编辑，当前工资政策或 Calendar 变化仍不会自动重算过去月份。
- 工资历史图与历史列表共享同一组 saved salary records；图表继续使用真实最终工资，金额、数据点、专属竖向虚线与月份标签保持同一横坐标，不补造缺失月份。历史卡片不再展示“额外收入”。
- Deployment = PASS；Cloudflare Access 登录跳转符合预期。已认证 browser automation 被 Windows sandbox helper 故障阻塞，因此生产视觉 smoke 保持 blocked，而非已观察到应用 regression。No migration，Production DB test writes = 0，未执行 QianJi action、D1 retry 或 baseline rebuild。

## 2026-09-13 Calendar Widget Phase 1 Final Acceptance

- Implementation commit `a53fd7a60415069f2ac7d1693f0835cce55ed55a` 正式部署为 Worker Version `8544d2b0-e347-4dbb-a6c7-bf9a45d3b390`，Deployment = 100%。
- 只读端点为 `GET /api/v1/calendar/widget`：复用 Calendar domain 作为唯一事实来源，只返回受限月度 DTO，不新增节假日真相源、mutation endpoint、schema 或 migration，D1 writes = 0。
- 独立 `Pulse Calendar Widget` Access Application 使用精确 path-scoped Service Auth；匿名与错误 token 为 403，正确 Widget token 为 200，POST/PUT/PATCH/DELETE 为 405，Widget token 访问首页、Annual、Calendar、Calendar Notes、Salary 均进入父级 Access 登录隔离。
- `workers_dev = false`、`preview_urls = false`；Cloudflare API确认 `enabled=false`、`previews_enabled=false`。Cloudflare Browser Rendering 独立出口的外部黑盒验证确认 canonical workers.dev、当前 Version Preview 与上一 Version Preview 均无法进入 Worker 业务处理路径，未返回 Widget JSON。404 仅为观测结果，不是安全机制；安全边界来自已关闭的 Worker subdomain 与 Preview URL 配置。
- Calendar targeted tests 20/20、full verify 219/219、production build 与 `git diff --check` 全部通过。full verify 曾出现一次 transient local-D1 timeout，单项重试及随后完整 219/219 均通过，归类为 `Non-blocking transient local test-environment failure`，不产生 D1 修复任务。
- `PHASE 1 PRODUCTION ACCEPTANCE = PASS`

## 2026-09-13 Health Ingest Recovery Final Closure

- Health 机器入口原为 `workers.dev/api/health`；Calendar Widget 安全收口将 `workers_dev = false`、`preview_urls = false` 后，Auto Export Health 因旧入口消失返回 404。Health handler 本身仍存在，故障根因是 machine-entry security regression，不是 HealthKit、Auto Export payload 或 D1 故障。
- 正式机器写入入口改为 `POST /api/health/ingest`；`/api/health` 继续保留 Dashboard/private read 边界，不再用同一路径的 HTTP method 区分机器写入与 Owner 读取。
- Health Ingest 使用独立 Cloudflare Access Application、独立 Health Service Token，并在 Worker 内继续校验独立 `X-API-Key`。Auto Export Health 请求头为 `CF-Access-Client-Id`、`CF-Access-Client-Secret`、`X-API-Key`；任何 Secret 均不进入文档或 Git。
- Calendar Token / Health Token isolation matrix 全部通过：Health token 只能到达 Health Ingest，Calendar token 只能读取 Calendar Widget；匿名请求、跨 endpoint token、Dashboard/private read 均按预期被 Access 隔离。canonical workers.dev 与 Preview URLs 继续关闭。
- iPhone 真机最终导入覆盖 `2026-09-07` 至 `2026-09-13` 共 7 天：`imported = 7`、`rowsInserted = 0`、`rowsUpdated = 7`；最新 Health record 正常返回，无重复插入。
- `HEALTH INGEST RECOVERY = PASS`、`TOKEN ISOLATION = PASS`、`HEALTH INGEST SECURITY ACCEPTANCE = PASS`、`DEVICE ACCEPTANCE = PASS`、`PHASE 1 PRODUCTION ACCEPTANCE = PASS`。

## 2026-09-13 QianJi Production D1 Read-only Recheck

- `2026-09-13T21:42:06+08:00` 执行 `SELECT 1 AS probe;`：exit code `0`，Cloudflare `success = true`、`changed_db = false`、`rows_written = 0`。
- `2026-09-13T21:42:22+08:00` 执行 `SELECT COUNT(*) AS finance_row_count FROM finance_transactions;`：exit code `0`，当前总行数 `1099`，Cloudflare `success = true`、`changed_db = false`、`rows_written = 0`。
- `2026-09-13T21:42:38+08:00` 执行 `SELECT COUNT(*) AS qianji_source_row_count FROM finance_transactions WHERE source = 'qianji';`：exit code `0`，QianJi source 行数 `1099`，Cloudflare `success = true`、`changed_db = false`、`rows_written = 0`。
- 三条命令均由 APAC/HKG 的 v3-prod primary 返回，未产生 Cloudflare error ID。`REMOTE D1 READ = PASS`、`QIANJI D1 PLATFORM BLOCK = CLEARED`、`QIANJI BASELINE REBUILD = NOT EXECUTED`、`D1 WRITES = 0`。
