---
title: Pulse 健康仪表盘开发记录
date: 2026-07-22
tags:
  - cloudflare
  - react
  - dashboard
  - development-log
---

# Pulse 健康仪表盘开发记录

## 项目目标

根据参考图片制作一个可部署到 Cloudflare 的健康训练仪表盘，包含训练结果、训练日历、每日步数、减重计划和习惯完成情况。

## 技术方案

- React 19 + TypeScript
- vinext / Vite
- Cloudflare Workers Static Assets
- Tailwind CSS 入口配合自定义 CSS
- 预留 Cloudflare D1 与 R2 绑定入口

## 已完成工作

### 2026-07-22：项目初始化

- 建立 Cloudflare 兼容的 vinext 项目结构。
- 配置 Vite Cloudflare 插件与 Worker 兼容的 ESM 构建。
- 保留 `.openai/hosting.json`，便于后续配置 D1 和 R2。

### 2026-07-22：界面实现

- 完成侧边导航和欢迎区域。
- 完成训练成果气泡图。
- 完成训练日历及训练状态标记。
- 完成步数环形进度和减重进度条。
- 完成健康习惯列表与完成次数显示。
- 添加桌面、平板和手机响应式布局。
- 添加中文页面元数据与基础无障碍标签。

### 2026-07-22：项目清理

- 移除初始加载骨架及其依赖。
- 更新项目名称和 README。
- 将初始模板测试替换为 Pulse 页面渲染测试。

### 2026-07-25：工资计算模块

- 新增侧边栏“工资计算”入口和响应式工资计算卡片。
- 支持输入 0–31 个工作日并即时更新工资结果。
- 应发工资按“工作日 × 275 元”计算。
- 税前固定扣除保险 60 元、房租 50 元和水电 20 元，共 130 元。
- 计税收入按“应发工资 − 130 元 − 5,000 元”计算，最低为 0 元。
- 个人所得税按计税收入的 3% 计算。
- 实发工资按“应发工资 − 130 元 − 个税”计算。
- 2026 年 7 月按 23 个工作日计算：应发 6,325 元、个税 35.85 元、实发 6,159.15 元。
- 补充服务器渲染测试，验证工资模块及默认计算结果。

### 2026-07-25：工作日历

- 将训练日历改为 2026 年 7 月工作日历。
- 修正七月为 31 天，并按星期一至星期日正确对齐日期。
- 区分已工作、待工作、周末和今天。
- 显示本月共 23 个工作日，与工资计算默认值保持一致。

## 主要文件

- `app/page.tsx`：页面结构与示例数据
- `app/globals.css`：视觉设计和响应式布局
- `app/layout.tsx`：页面元数据和根布局
- `vite.config.ts`：Cloudflare/Vite 构建配置
- `.openai/hosting.json`：Cloudflare 资源绑定声明
- `tests/rendered-html.test.mjs`：服务器渲染测试

## 当前状态

- Node.js、npm、Git 和 GitHub CLI 已可用。
- GitHub 公开仓库：`braziliany/brilliant-life`。
- Cloudflare Worker 与自定义域名已部署。
- `pulse.sophier.org` 已通过 Cloudflare Access 保护。

## 后续计划

- 增加月度工资记录和历史趋势。
- 如需跨设备保存数据，再接入 Cloudflare D1。

### 2026-07-25：Apple Watch / Apple 健康数据接入

- 启用 Cloudflare D1，新增按日期保存的健康数据表。
- 新增 `/api/health`：GET 读取最近一次汇总，POST 接收 iPhone 健康数据。
- 上传接口使用 `X-API-Key` 独立鉴权，并限制各项数值范围。
- 首期字段包括步数、活动能量、锻炼分钟数、训练次数与数据来源。
- 仪表盘会优先显示已同步的步数、活动能量和运动时长；未同步时显示示例值及提示。
- 手机端推荐使用 Health Auto Export，将 JSON 定时发送到 Worker 地址。
- 接口兼容 Health Auto Export Version 2 的 `data.metrics` 嵌套结构以及旧版顶层 `metrics` 数组，可识别 `step_count`、`active_energy` 和 Apple 锻炼时间，并按日期汇总写入。
